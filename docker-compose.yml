services:
  website:
    image: nginx:alpine
    container_name: bb84-website
    ports:
      - "8089:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
